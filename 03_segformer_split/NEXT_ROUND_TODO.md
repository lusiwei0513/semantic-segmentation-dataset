# 下一轮训练提醒清单（side 结束后必须提醒用户）

> **给父代理**：当前 side_fold0（或侧视）训练结束后，**必须**提醒用户完成本清单，再规划/启动下一轮。  
> **注意**：勿与正在运行的 side 512/384 训练混跑或改配置。

---

## 1. 640 分辨率重训（必做 — 用户已确认）

- [ ] 当前 **side_fold0** 跑完后，提醒用户并规划/启动 **front**（最好同时有可对比的 side）在 **front_size [640, 640]** 上的训练。
- [ ] 在文档中写明 **side 分辨率选型**（与 front 640 对齐或另行记录）。
- [ ] 对「原来的模型」路径也做 640：**重评/重训**，便于与此前 **512** 跑次对比。
- [ ] **显存**：RTX 3050 4GB 上可能需 `batch_size=1` + **梯度累积**（grad_accum）。
- [ ] **禁止**与当前正在跑的 side **512/384** 训练混在同一配置/同一进程中改分辨率。

---

## 2. 可视化 / 推理通道规则（必做）

| 视角 | 绘制 / 预测（有效通道） | 屏蔽（不画 / 不作为有效输出） |
|------|-------------------------|--------------------------------|
| **Front** | body, windshield, nose_tip | bogie, door |
| **Side**  | body, windshield, bogie, door | nose_tip |

- [x] 修复 `evaluate_test` / 可视化：只显示**有效通道**；front 含 tip；已按 `valid_seg_tasks` 屏蔽正视 bogie/door（2026-07-22 重导 test compare）。
- [x] Side 可视化不得画 nose_tip；Front 不得画 bogie / door。

---

## 3. 可选后处理（说明，按需开启）

| 项 | 含义 | 建议 |
|----|------|------|
| **Threshold** | sigmoid 分数截断，默认 **0.5** | 可视化可先试 **body = 0.4** |
| **Hole-fill** | 对 body mask 做一次 **3×3 形态学闭运算** | 填小孔，仅影响展示/后处理 mask |
| **相关文件** | `src/utils/visualization.py`、`scripts/visualize_predictions.py` | 改展示逻辑优先动这两处 |
| **指标** | `official_metrics` | **仅当故意要改上报的 mIoU** 时才动；默认不要为了好看改官方指标 |

---

## 执行顺序建议

1. 等 side 当前轮次结束（勿中断）。
2. 提醒用户：640 重训 + 通道可视化修复。
3. 单独起 640 实验；再修 viz / evaluate_test 通道规则；后处理按需。
